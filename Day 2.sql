WITH UNIONED_TABLE AS (
SELECT
	ID,
    CHR(VALUE)
FROM
	LETTERS_A
 UNION ALL
 SELECT
 	ID,
    CHR(VALUE)
FROM
	LETTERS_B
)
SELECT
	STRING_AGG(CHR, '' ORDER BY ID)
FROM UNIONED_TABLE
WHERE CHR ~* '[a-zA-Z, ,!,",'',(,),-,.,:,;,?]'

